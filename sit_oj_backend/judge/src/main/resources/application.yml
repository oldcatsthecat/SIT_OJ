server:
  port: 8083

spring:
  application:
    name: judge  # 简洁的名字
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

oj:
  judge:
    server-url: http://123.60.72.80:8080
    token: CHANGE_THIS

# JWT 配置
jwt:
  # 必须至少 32 个字符，确保 HS256 算法安全
  secret: SIT_ALGORITHM_COMMUNITY_SECRET_KEY_2026_SECURE_VERSION
  # 过期时间，单位：毫秒 (86400000 = 24小时)
  expiration: 86400000